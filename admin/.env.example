# Where the notification API is deployed (the Vercel project in `server/`).
# Vite inlines VITE_ vars at build time, so this ends up in the browser bundle —
# which is fine, it is only a hostname. The service-account credential lives on
# the server and never comes near this build.
VITE_NOTIFY_URL=https://your-project.vercel.app
